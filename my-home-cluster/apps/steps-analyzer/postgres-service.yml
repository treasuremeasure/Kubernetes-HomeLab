apiVersion: v1
kind: Service
metadata:
  name: steps-postgres
  labels:
    app: steps-postgres
spec:
  type: ClusterIP
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: steps-postgres
